"use strict";

const assert = require("assert");

describe("Testeando SalesDB", () => {
    it("Deberia 1 ser igual a 1", () => {
        assert.equal(1 , 1);
    });
    it("Deberia 2 ser igual a 2", () => {
        assert.equal(2 , 2);
    });
});
